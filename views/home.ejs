<%- include('partials/header'); -%>

    <h1>Home</h1>

    <p>
        <%= homeContent %>
    </p>


    <!-- <% for(var i=0; i<postDec.length; i++){ %>
    <div class="item">
        <h3>
            <%= postDec[i].title %>
    </h3>
    <p><%= postDec[i].data %></p>
    </div>
    <% } %> -->

    <% postDec.forEach((element)=>{ %>
        <div class="item">
            <h1>
                <%= element.title %>
            </h1>
            <p>
                <%= element.content.substring(0,100) + "..."%>
                <a href= "/posts/<%= element._id %> " >Read More</a>
            </p>
        </div>
        <% }) %>

            <%- include('partials/footer'); -%>
            